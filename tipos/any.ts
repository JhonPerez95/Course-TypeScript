;(() => {
  let avenger: any = 123

  // Por defecto, el tipo de dato any es una variable que puede contener cualquier tipo de dato
  let exists

  // en typescript cuando se crea una  constante se tiene que iniciarlizar con un valor
  // const power
  let power

  avenger = 'Thor'
  console.log(avenger.charAt(0))
})()
